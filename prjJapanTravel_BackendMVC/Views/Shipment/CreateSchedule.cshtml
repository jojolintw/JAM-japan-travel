@model prjJapanTravel_BackendMVC.Models.Schedule

@{
    ViewData["Title"] = "Create Schedule";
}

<h1>新增航班</h1>

<div class="row">
    <div class="col-md-6">
        <form asp-action="CreateSchedule">
            <!-- RouteId 手動輸入 -->
            <div class="form-group">
                <label asp-for="RouteId" class="control-label">航線編號</label>
                <input asp-for="RouteId" class="form-control" placeholder="Enter Route ID" />
                <span asp-validation-for="RouteId" class="text-danger"></span>
            </div>

            <!-- Departure Time -->
            <div class="form-group">
                <label asp-for="DepartureTime" class="control-label">出發時間</label>
                <input asp-for="DepartureTime" class="form-control"
                       placeholder="Enter Departure Time"
                       value="@Model.DepartureTime.ToString("yyyy-MM-dd HH:mm")" /> <!-- 顯示到分鐘 -->
                <span asp-validation-for="DepartureTime" class="text-danger"></span>
            </div>

            <!-- Arrival Time -->
            <div class="form-group">
                <label asp-for="ArrivalTime" class="control-label">到達時間</label>
                <input asp-for="ArrivalTime" class="form-control"
                       placeholder="Enter Arrival Time"
                       value="@Model.ArrivalTime.ToString("yyyy-MM-dd HH:mm")" /> <!-- 顯示到分鐘 -->
                <span asp-validation-for="ArrivalTime" class="text-danger"></span>
            </div>

            <!-- Seats -->
            <div class="form-group">
                <label asp-for="Seats" class="control-label">剩餘座位數</label>
                <input asp-for="Seats" class="form-control" placeholder="Enter Seats Available" />
                <span asp-validation-for="Seats" class="text-danger"></span>
            </div>

            <!-- Capacity -->
            <div class="form-group">
                <label asp-for="Capacity" class="control-label">總座位數</label>
                <input asp-for="Capacity" class="form-control" placeholder="Enter Capacity" />
                <span asp-validation-for="Capacity" class="text-danger"></span>
            </div>

            <!-- Submit Button -->
            <div class="form-group">
                <input type="submit" value="儲存並新增" class="btn btn-primary" />
                <button type="button" class="btn btn-secondary" onclick="fillDemoData()">DEMO</button>

            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-secondary">返回</a>
</div>
<script>
    function fillDemoData() {
        // 假資料
        const demoData = {
            routeId: 1, // 假設的航線編號
            departureTime: new Date().toISOString().slice(0, 16), // 當前時間（到分鐘）
            arrivalTime: new Date(new Date().getTime() + 2 * 60 * 60 * 1000).toISOString().slice(0, 16), // 當前時間加兩小時
            seats: 20, // 假設的剩餘座位數
            capacity: 50 // 假設的總座位數
        };

        // 填入假資料
        document.getElementById("RouteId").value = demoData.routeId;
        document.getElementsByName("DepartureTime")[0].value = demoData.departureTime;
        document.getElementsByName("ArrivalTime")[0].value = demoData.arrivalTime;
        document.getElementsByName("Seats")[0].value = demoData.seats;
        document.getElementsByName("Capacity")[0].value = demoData.capacity;
    }
</script>